<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Plataforma Perguntas e Respostas</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <img src="/images/guia.png" alt="Guia do Programador">
    <h1>Guia Perguntas</h1>
    <hr />
    <h3>Lista de Perguntas</h3>

    <p><strong>Nome:</strong> <%= nome %></p>

    <p><strong>Linguagem:</strong> <%= lang %></p>

    <p><strong>Empresas:</strong> <%= empresas %></p>

    <p>
      <strong>Empresas em lista:</strong>
      <% if(empresas.length > 0) { %>
        <% for(i = 0; i < empresas.length; i++) { %>
          <ul>
            <li>
              <%= empresas[i] %>
            </li>
          </ul>
        <% } %>
      <% } else { %>
        <h3>Não há empresa cadastrada para esse usuário!</h3>
      <% } %>
    </p>

    <% if(msg == true) { %>
      <h3>Isso é uma mensagem de erro!</h3>
    <% } else { %>
      <h3>Nenhum erro!</h3>
    <% } %>

    <p>Estrutura de repetição com <strong>forEach:</strong></p>
    <hr>
    <% produtos.forEach(function(produto) { %>
      <h3><%= produto.nome %></h3>
      <h4>R$ <%= produto.preco %></h4>
      <hr>
    <% }) %>

    <p>Estrutura de repetição com <strong>for:</strong></p>
    <% for(i = 0; i < produtos.length; i++) { %>
      <%= produtos[i].nome %>
      R$ <%= produtos[i].preco %>
      <br>
    <% } %>

  </body>
</html>
